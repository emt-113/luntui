【角色设定】 你是一位精通嵌入式控制系统的资深工程师。我现有的平衡小车代码使用的是“单环 PD”控制，我现在希望将其升级为**“串级 PID（双环）”控制结构**。

【任务目标】 请修改我提供的 control.c 代码，将 control_run_1ms 函数重构为**角度环（外环）+ 角速度环（内环）**的串级控制逻辑。

【技术要求】

架构设计：

外环（角度环）：输入是角度误差 (Target - Current_Pitch)，输出是目标角速度。

内环（角速度环）：输入是角速度误差 (Target_Gyro - Current_Gyro)，输出是电机 PWM。

注意： 请复用我代码中已有的 pid_compute 函数来计算这两个环（需要定义两个 PID 结构体：balance_angle_pid 和 balance_gyro_pid）。

硬件约束（绝对不可修改！）：

机械零点：-10.6f。

IMU 方向：车后仰时 Pitch 变大（正值）。

PWM 极性：

计算出的最终 Output 如果 > 0（代表车后仰，需要后退），则：

左轮 PWM = (int16_t)Output (正)

右轮 PWM = -(int16_t)Output (负)

驱动接口：保持使用 small_driver_set_duty(left, right)。

代码修改点：

在 control_init 中初始化两个 PID 结构体（参数先给 0 或典型值）。

在 control_run_1ms 中实现串级逻辑。

重要： 请保留我原有的“倒地保护”和“自动恢复”逻辑。

【我的当前代码 (control.c)