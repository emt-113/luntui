你好！我正在开发一款 V型五连杆轮腿机器人（倒立摆架构）。目前速度环已调通，基础高度固定为 4.4cm，腿部机械伸展极限为 14.5cm。

现在我需要你帮我编写基于“单边不对称代偿”的横滚角（Roll）防护代码。请严格遵循以下物理设定和控制逻辑，并尽量复用现有文件中的变量定义。

【物理设定与逻辑】

Roll 轴方向： Roll < 0° 代表车身左侧偏低，右侧偏高。

控制策略（单边只伸不缩）：

车身倾斜时，较高一侧的腿保持基础高度（4.4cm）定死不动。

较低一侧的腿根据 Roll 的误差（PD控制）主动伸长，以顶平车身。最大不能超过 14.5cm。

分级保护阈值：

安全死区 (-2.5° ~ 2.5°)： 忽略误差，左右腿均保持 4.4cm。

动态代偿区 (2.5° ~ 45° 或 -2.5° ~ -45°)： 启动单边伸长代偿逻辑。

失控危险区 (> 45° 或 < -45°)： 触发系统停机保护，强制关闭舵机和电机输出，防止堵转。

【需要修改的文件与重构要求】

1. vmc.c (运动学查表核心)：



2. control.c (控制逻辑层)：

编写 Roll_Protection_Compute() 函数，实现上述的分级保护与 PD 计算。

输出平滑处理后的 Height_L 和 Height_R。

请复用现有的 IMU 变量（假设为 icm_output.roll ）和系统状态机变量（如 g_System_State）。

3. Servo.c (舵机执行层)：

提供一个将 vmc.c 算出的单侧舵机 PWM 映射到具体 4 个舵机的执行接口。

【代码输出要求】

只需要输出新增或修改的函数代码片段，不需要写满整个文件。

变量命名需规范（例如加入平滑滤波防止腿部突变），并加上详细的中文注释说明控制流向。