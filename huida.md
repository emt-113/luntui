* 浮浮酱，请选择 **选项 C**，我已经为你准备好了所有问题的具体答案，请基于以下决策开始编写代码：

  1. **编码规范**：
     - 严格使用 **下划线命名法 (snake_case)**，例如 `control_run_1ms()`, `pid_compute()`，保持与现有代码风格一致。

  2. **PID 参数与限制**：
     - **初始值**：请将 `Kp`, `Ki`, `Kd` 的默认宏定义全部设为 **0.0f**。安全第一，我会在调试时手动增加。
     - **积分项**：直立环暂时只需要 PD 控制，`Ki` 设为 0，但请在结构体中保留积分接口以备后续速度环使用。

  3. **调试与数据**：
     - 请将核心过程数据定义为全局变量，方便我在外部串口任务中观测：
       - `float g_Debug_Pitch;` (当前角度)
       - `float g_Debug_Gyro;` (当前角速度)
       - `int16_t g_Debug_PWM;` (计算出的最终输出)
       - `uint8_t g_System_State;` (0=STOP, 1=RUN)

  4. **舵机控制 (关键)**：
     - **中位值**：每个舵机因为物理原因都有不同的中值，具体在servo.h，servo.c里
     - **函数接口**：你可以看看，但是我建议初始化后舵机先不调整，因为我的机械零点已经测好了
     - **逻辑**：在系统处于 RUN 状态时，持续将 4 个舵机锁死在 对应的中值，确保腿部不软。

  5. **Gyro 单位修正 (重要)**：
     - 我的 `icm_output.gyro_y`数值范围较小（约 -5~5）。
     - 请在 PID 计算前增加一个**放大系数宏** `GYRO_SCALE` (默认 1.0f)，方便我后续调整量级。

  6. **引用头文件**：
     - 只需要 `#include "bsp_app.h"`，假设所有驱动和结构体 `icm_output_t` 都在这里面。
     - 代码都在user和code里，你添加的话就在code里吧，

  请开始编写 `pid.c/.h` 和 `control.c/.h`。